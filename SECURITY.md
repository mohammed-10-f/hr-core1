# Security baseline

- كلمات المرور تستخدم PBKDF2-HMAC-SHA256 بواقع 100,000 iteration.
- الجلسات تستخدم token عشوائياً 256-bit تقريباً وتخزن في D1 مع وقت انتهاء وإلغاء.
- جميع API الحساسة تتطلب session وpermission.
- مدير النظام يملك الصلاحيات المزروعة افتراضياً ويمكن إدارة الأدوار لاحقاً.
- كل عملية حساسة تسجل في `audit_logs`.
- لا يتم تخزين كلمة المرور كنص صريح.
- بيانات الدخول الأولية `admin / 1234` مخصصة للـ bootstrap ويجب تغييرها فوراً.
- لا تضع أسرار Cloudflare أو مفاتيح API في GitHub.
