# D1 Schema Manager

هذه النسخة تستخدم **Canonical Schema** واحداً ولا تحمل أي افتراضات من قواعد HR Core السابقة.

## قواعد التوافق
1. كل الـ IDs الداخلية `INTEGER`.
2. أسماء الحقول عربية/إنجليزية حسب الغرض، ولا يوجد alias مثل `name` و`name_ar` لنفس المفهوم في الطبقة الجديدة.
3. الـ Worker هو المسؤول عن bootstrap والتأكد من وجود الجداول والفهارس.
4. `schema_migrations` يسجل الإصدار.
5. الإصلاح التلقائي **إضافي وغير هدّام**: إنشاء جدول، إضافة عمود، إضافة index.
6. لا يوجد حذف تلقائي للجداول أو الأعمدة أو بيانات HR.
7. تغييرات الأنواع أو إعادة الهيكلة الكبرى يجب أن تكون Migration صريحة ومراجعة قبل النشر.

## الأداء
`ensureSchema()` يستخدم promise على مستوى Worker isolate، لذلك لا يعاد بناء الـ schema في كل طلب. على قاعدة مستقرة، تسجيل الدخول لا يشغل سلسلة طويلة من introspection.

## تطور النسخ
عند إضافة بنية جديدة:
- أضف migration مرقمة.
- ارفع `schema_version`.
- أضف العمود/الجدول إلى manifest.
- اختبر migration على نسخة D1 نظيفة ونسخة تحتوي بيانات تجريبية.

## ممنوع
لا تستخدم Schema من أي إصدار سابق على هذه القاعدة.
